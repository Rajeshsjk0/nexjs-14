import React from "react";

const Header = () => {
  return (
    <header
      style={{
        backgroundColor: "lightblue",
        padding: "1rem",
      }}
    >
      <p>header</p>
    </header>
  );
};

export default Header;
