import React from "react";

const LineChart = () => {
  return (
    <div>
      <h1>
        LineChart this cannot be accessed directly need to import in page.tsx
      </h1>
    </div>
  );
};

export default LineChart;
